{
  "name": "@hecaton/shell",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/main/main.js",
  "scripts": {
    "build": "tsc --build && node scripts/copy-renderer-assets.mjs",
    "start": "npm run build && electron ."
  },
  "dependencies": {
    "@hecaton/browser-engine": "*",
    "@hecaton/core": "*",
    "@hecaton/games": "*",
    "@hecaton/storage": "*",
    "@hecaton/window-manager": "*"
  },
  "devDependencies": {
    "electron": "43.2.0"
  }
}
